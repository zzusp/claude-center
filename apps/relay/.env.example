# apps/relay（SSE 中转服务，可选实时线）环境变量示例。
# 复制为同目录 .env 后填值；留空的可选项用默认值。各服务共用一份根 .env 也可（见仓库根 .env.example），
# 此文件只列 relay 实际读取的变量。完整方案见 docs/spec/sse-relay-service.md。
# 注意：relay 不连数据库（DB 由 Console/Worker 读写后 best-effort publish 给中转），故无需 DATABASE_URL。

# Console + relay 共享：浏览器订阅票据的 HMAC 签名密钥（自定义随机串，越长越好）。空=拒绝一切票据订阅。
CLAUDE_CENTER_RELAY_SECRET=change-me-to-a-long-random-secret
# Console + Worker + relay 共享：发布事件的鉴权 token。空=拒绝一切发布。
CLAUDE_CENTER_RELAY_PUBLISH_TOKEN=change-me-publish-token
# Worker + relay 共享：Worker 订阅中转的鉴权 token。空=拒绝 worker token 订阅。
CLAUDE_CENTER_RELAY_WORKER_TOKEN=change-me-worker-token

# 可选。监听地址与端口（默认 127.0.0.1:8787）。
# CLAUDE_CENTER_RELAY_HOST=127.0.0.1
# CLAUDE_CENTER_RELAY_PORT=8787
# 可选。保活 :ping 间隔（ms，默认 15000）与每频道 Last-Event-ID 重放环容量（默认 200）。
# CLAUDE_CENTER_RELAY_PING_INTERVAL_MS=15000
# CLAUDE_CENTER_RELAY_RING_SIZE=200
