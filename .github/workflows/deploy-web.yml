# cc-vX.Y.Z tag → Web 端部署到生产服务器（docker compose build/up）→ 创建 GitHub Release。
# 详细方案见 docs/spec/deployment-pipeline.md。
name: deploy-web

on:
  push:
    tags:
      - 'cc-v[0-9]+.[0-9]+.[0-9]+'

# 同一时间只允许一个 deploy-web 在跑（避免两个 tag 并发部署互踩 git checkout）。
concurrency:
  group: deploy-web
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      app_version: ${{ steps.parse.outputs.app_version }}
      release_notes_path: ${{ steps.notes.outputs.path }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Parse tag → APP_VERSION
        id: parse
        run: |
          ref="${GITHUB_REF#refs/tags/}"
          version="${ref#cc-v}"
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::tag $ref 不符合 cc-vX.Y.Z 格式"
            exit 1
          fi
          echo "app_version=$version" >> "$GITHUB_OUTPUT"
          echo "[parse] APP_VERSION=$version"

      - name: Install deps
        run: npm ci --ignore-scripts

      - name: Typecheck
        run: npm run typecheck

      - name: Build (smoke)
        env:
          NEXT_PUBLIC_APP_VERSION: ${{ steps.parse.outputs.app_version }}
        run: |
          npm -w @claude-center/db run build
          npm -w @claude-center/relay-client run build
          npm -w @claude-center/console run build
          npm -w @claude-center/relay run build

      - name: Extract release notes
        id: notes
        run: |
          mkdir -p artifacts
          node scripts/extract-changelog.mjs CHANGELOG-console.md "${{ steps.parse.outputs.app_version }}" > artifacts/notes.md
          echo "path=artifacts/notes.md" >> "$GITHUB_OUTPUT"
          echo "── release notes ──"; cat artifacts/notes.md; echo "───────────────"

      - name: Upload notes artifact
        uses: actions/upload-artifact@v4
        with:
          name: release-notes
          path: artifacts/notes.md
          retention-days: 7

  deploy:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: DEPLOY
    steps:
      - name: Checkout @ tag
        # 在 runner 上 checkout 当前 tag，作为 release bundle 的来源。runner 在境外能直连 github.com，
        # 服务器对 github.com:443 不通（国内常见），所以服务器不再 git fetch，而是接收 scp 上来的 tarball。
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_SSH_KEY }}

      - name: Trust host
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_KNOWN_HOSTS }}" >> ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts

      - name: Pack release bundle
        env:
          APP_VERSION: ${{ needs.build.outputs.app_version }}
        run: |
          set -e
          # 把 runner 上 checkout 出的 tag 内容打包。排除 .git / node_modules / 产物。
          # bundle 解压后的根目录直接是仓库内容（不带 wrapper 目录），与 deploy-on-server.sh 的解析一致。
          tar czf /tmp/cc-deploy-${APP_VERSION}.tar.gz \
            --exclude='node_modules' --exclude='.next' --exclude='dist' \
            --exclude='.git' --exclude='apps/worker/release' --exclude='docs/tmp' \
            --exclude='.env' --exclude='.env.*' \
            .
          ls -la /tmp/cc-deploy-${APP_VERSION}.tar.gz

      - name: Upload bundle + deploy script
        env:
          APP_VERSION: ${{ needs.build.outputs.app_version }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
        run: |
          set -e
          scp -P "${DEPLOY_PORT}" -o StrictHostKeyChecking=yes /tmp/cc-deploy-${APP_VERSION}.tar.gz \
            "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/"
          # deploy-on-server.sh 也走 /tmp/ 版本（避免「先 rsync 替换才能拿到新版脚本」的鸡生蛋）。
          scp -P "${DEPLOY_PORT}" -o StrictHostKeyChecking=yes scripts/deploy-on-server.sh \
            "${DEPLOY_USER}@${DEPLOY_HOST}:/tmp/deploy-on-server.sh"

      - name: Run deploy-on-server.sh
        env:
          APP_VERSION: ${{ needs.build.outputs.app_version }}
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
        run: |
          set -e
          ssh -o StrictHostKeyChecking=yes -p "${DEPLOY_PORT}" "${DEPLOY_USER}@${DEPLOY_HOST}" \
            "sudo bash /tmp/deploy-on-server.sh ${APP_VERSION} /tmp/cc-deploy-${APP_VERSION}.tar.gz"

      - name: External health check
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
        run: |
          # 部署完后从 CI runner 再次走公网验证 console 可达（绕开服务器内 127.0.0.1）。
          # 401 与 200 都算 OK；500 / 超时 / 拒连 → fail。
          set -e
          for i in 1 2 3 4 5; do
            code=$(curl -s -o /dev/null -m 5 -w '%{http_code}' "http://${DEPLOY_HOST}:3000/api/auth/me" || echo 000)
            if [[ "$code" == "200" || "$code" == "401" ]]; then
              echo "console health OK (HTTP $code)"
              exit 0
            fi
            echo "尝试 $i：HTTP $code，5s 后重试"
            sleep 5
          done
          echo "::error::console 外部健康检查未通过（最后 HTTP $code）"
          exit 1

  release:
    needs: [build, deploy]
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download release notes
        uses: actions/download-artifact@v4
        with:
          name: release-notes
          path: artifacts

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          gh release create "$TAG" \
            --title "$TAG" \
            --notes-file artifacts/notes.md \
            --target "$GITHUB_SHA"
